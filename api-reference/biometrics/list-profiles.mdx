---
title: "List Voice Profiles"
description: "Retrieve all voice biometrics profiles for your tenant"
api: "GET https://api.talkos.io/api/voice-biometrics"
---

# List Voice Profiles

Retrieve a paginated list of all voice biometrics profiles enrolled for your tenant, with optional filtering by status.

## Endpoint

```
GET /api/voice-biometrics
```

## Headers

<ParamField header="x-api-key" type="string" required>
  Your TalkOS API key for authentication
</ParamField>

<ParamField header="x-tenant-id" type="string" required>
  Your unique tenant identifier
</ParamField>

## Query Parameters

<ParamField query="status" type="string">
  Filter profiles by status: `pending`, `enrolled`, `active`, `suspended`
</ParamField>

<ParamField query="page" type="number" default="1">
  Page number for pagination
</ParamField>

<ParamField query="limit" type="number" default="20">
  Number of profiles per page (max: 100)
</ParamField>

<ParamField query="sortBy" type="string" default="createdAt">
  Field to sort by: `createdAt`, `updatedAt`, `callerName`, `verificationCount`
</ParamField>

<ParamField query="sortOrder" type="string" default="desc">
  Sort order: `asc` or `desc`
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Whether the request was successful
</ResponseField>

<ResponseField name="profiles" type="array">
  Array of voice print profiles
</ResponseField>

<ResponseField name="pagination" type="object">
  Pagination metadata
</ResponseField>

<ResponseField name="pagination.page" type="number">
  Current page number
</ResponseField>

<ResponseField name="pagination.limit" type="number">
  Number of results per page
</ResponseField>

<ResponseField name="pagination.total" type="number">
  Total number of profiles
</ResponseField>

<ResponseField name="pagination.totalPages" type="number">
  Total number of pages
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://api.talkos.io/api/voice-biometrics?status=active&page=1&limit=20" \
  -H "x-api-key: your-api-key" \
  -H "x-tenant-id: your-tenant-id"
```

```javascript JavaScript
const params = new URLSearchParams({
  status: 'active',
  page: '1',
  limit: '20'
});

const response = await fetch(`https://api.talkos.io/api/voice-biometrics?${params}`, {
  method: 'GET',
  headers: {
    'x-api-key': 'your-api-key',
    'x-tenant-id': 'your-tenant-id'
  }
});

const data = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://api.talkos.io/api/voice-biometrics',
    headers={
        'x-api-key': 'your-api-key',
        'x-tenant-id': 'your-tenant-id'
    },
    params={
        'status': 'active',
        'page': 1,
        'limit': 20
    }
)

data = response.json()
```
</RequestExample>

<ResponseExample>
```json Success Response
{
  "success": true,
  "profiles": [
    {
      "voicePrintId": "vp_abc123def456",
      "callerId": "+14155551234",
      "callerName": "John Smith",
      "status": "active",
      "samplesCount": 5,
      "verificationCount": 12,
      "successfulVerifications": 11,
      "lastVerifiedAt": "2026-02-17T16:45:00.000Z",
      "createdAt": "2026-01-15T09:00:00.000Z",
      "updatedAt": "2026-02-17T16:45:00.000Z"
    },
    {
      "voicePrintId": "vp_def456ghi789",
      "callerId": "+14155559876",
      "callerName": "Jane Doe",
      "status": "active",
      "samplesCount": 3,
      "verificationCount": 8,
      "successfulVerifications": 8,
      "lastVerifiedAt": "2026-02-18T10:15:00.000Z",
      "createdAt": "2026-02-01T14:30:00.000Z",
      "updatedAt": "2026-02-18T10:15:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 45,
    "totalPages": 3
  }
}
```

```json Empty Response
{
  "success": true,
  "profiles": [],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 0,
    "totalPages": 0
  }
}
```
</ResponseExample>

## Profile Status Values

| Status | Description |
|--------|-------------|
| `pending` | Profile created but needs more voice samples |
| `enrolled` | Sufficient samples collected, processing |
| `active` | Profile ready for verification |
| `suspended` | Profile temporarily disabled |

## Notes

<Info>
The list does not include full verification history for each profile. Use the [Get Voice Profile](/api-reference/biometrics/get-profile) endpoint for detailed profile information.
</Info>

<Tip>
Use the `sortBy=verificationCount&sortOrder=desc` parameters to identify your most frequently verified callers.
</Tip>
