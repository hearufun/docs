---
title: "End Coaching Session"
description: "Terminate an active coaching session"
api: "POST https://api.talkos.io/api/coaching/:coachingId/end"
---

# End Coaching Session

Terminate an active coaching session. This will stop the coaching connection between the supervisor and agent while the call may continue.

## Endpoint

```
POST /api/coaching/:coachingId/end
```

## Headers

<ParamField header="x-api-key" type="string" required>
  Your TalkOS API key for authentication
</ParamField>

<ParamField header="x-tenant-id" type="string" required>
  Your unique tenant identifier
</ParamField>

## Path Parameters

<ParamField path="coachingId" type="string" required>
  Unique identifier of the coaching session to end
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Whether the coaching session was ended successfully
</ResponseField>

<ResponseField name="coachingId" type="string">
  The coaching session ID that was ended
</ResponseField>

<ResponseField name="status" type="string">
  Final status of the coaching session: `ended`
</ResponseField>

<ResponseField name="startedAt" type="string">
  ISO 8601 timestamp when the coaching session started
</ResponseField>

<ResponseField name="endedAt" type="string">
  ISO 8601 timestamp when the coaching session ended
</ResponseField>

<ResponseField name="duration" type="number">
  Total duration of the coaching session in seconds
</ResponseField>

<ResponseField name="messagesCount" type="number">
  Total number of messages sent during the session
</ResponseField>

<ResponseField name="summary" type="object">
  Session summary statistics

  <Expandable title="summary properties">
    <ResponseField name="suggestions" type="number">
      Number of suggestion messages sent
    </ResponseField>

    <ResponseField name="alerts" type="number">
      Number of alert messages sent
    </ResponseField>

    <ResponseField name="scripts" type="number">
      Number of script messages sent
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://api.talkos.io/api/coaching/coach-789xyz/end \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-api-key" \
  -H "x-tenant-id: your-tenant-id"
```

```javascript JavaScript
const response = await fetch('https://api.talkos.io/api/coaching/coach-789xyz/end', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': 'your-api-key',
    'x-tenant-id': 'your-tenant-id'
  }
});

const data = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.talkos.io/api/coaching/coach-789xyz/end',
    headers={
        'Content-Type': 'application/json',
        'x-api-key': 'your-api-key',
        'x-tenant-id': 'your-tenant-id'
    }
)

data = response.json()
```
</RequestExample>

<ResponseExample>
```json Success Response
{
  "success": true,
  "coachingId": "coach-789xyz",
  "status": "ended",
  "startedAt": "2026-02-18T14:30:00.000Z",
  "endedAt": "2026-02-18T14:45:30.000Z",
  "duration": 930,
  "messagesCount": 12,
  "summary": {
    "suggestions": 7,
    "alerts": 2,
    "scripts": 3
  }
}
```

```json Error Response
{
  "success": false,
  "error": "Coaching session not found",
  "code": "SESSION_NOT_FOUND"
}
```
</ResponseExample>

## Error Codes

| Code | Description |
|------|-------------|
| `SESSION_NOT_FOUND` | The coaching session does not exist |
| `SESSION_ALREADY_ENDED` | The coaching session has already been terminated |
| `UNAUTHORIZED` | Invalid API key or tenant ID |
